import {test,expect} from '@playwright/test';
test('FPS ready, start, move, look, fire, score, pause and restart',async({page})=>{await page.goto('/');await page.waitForFunction(()=>window.__GAME_READY__===true);await page.evaluate(()=>window.__GAME__.start());const before=await page.evaluate(()=>window.__GAME__.entities.get('pacman').position.z);await page.keyboard.down('KeyW');await page.waitForTimeout(150);await page.keyboard.up('KeyW');expect(await page.evaluate(()=>window.__GAME__.entities.get('pacman').position.z)).toBeLessThan(before);await page.evaluate(()=>window.__GAME__.setView(.2,.1));expect((await page.evaluate(()=>window.__GAME__.getView())).yaw).toBe(.2);await page.evaluate(()=>{const g=window.__GAME__,p=g.entities.get('pacman').position;for(const id of [...g.entities.keys()])if(id.startsWith('invader-'))g.entities.delete(id);g.setView(0,0);g.entities.set('invader-test',{kind:'invader',position:{x:p.x,z:p.z-1},velocity:{x:0}});g.fire(performance.now()+1000)});expect(await page.evaluate(()=>window.__GAME__.gameplay.getState().score)).toBe(100);await page.keyboard.press('KeyP');expect(await page.evaluate(()=>window.__GAME__.gameplay.getState().status)).toBe('paused');await page.keyboard.press('KeyR');expect(await page.evaluate(()=>window.__GAME__.gameplay.getState().status)).toBe('playing')});

const overlaps=(a,b)=>a.x<b.x+b.width&&a.x+a.width>b.x&&a.y<b.y+b.height&&a.y+a.height>b.y;
for(const viewport of [{name:'portrait',width:390,height:844},{name:'compact landscape',width:844,height:390}])test(`touch ${viewport.name} keeps HUD, status and controls separate`,async({browser})=>{const context=await browser.newContext({viewport:{width:viewport.width,height:viewport.height},hasTouch:true,isMobile:true});const page=await context.newPage();await page.goto('/');await page.waitForFunction(()=>window.__GAME_READY__===true);await expect(page.locator('#touch-controls')).toBeVisible();const status=await page.locator('#status').boundingBox();const look=await page.locator('#look-zone').boundingBox();const hud=await page.locator('.pacman-hud').boundingBox();const dpad=await page.locator('.dpad').boundingBox();expect(overlaps(status,look)).toBe(false);expect(overlaps(hud,dpad)).toBe(false);expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth)).toBe(true);for(const button of await page.locator('#touch-controls button').all()){const box=await button.boundingBox();expect(box.width).toBeGreaterThanOrEqual(48);expect(box.height).toBeGreaterThanOrEqual(48)}await expect(page.locator('#crosshair')).toBeVisible();await context.close()});
